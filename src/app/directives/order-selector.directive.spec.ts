import { OrderSelectorDirective } from './order-selector.directive';

describe('OrderSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
