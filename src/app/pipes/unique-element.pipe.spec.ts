import { UniqueElementPipe } from './unique-element.pipe';

describe('UniqueElementPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueElementPipe();
    expect(pipe).toBeTruthy();
  });
});
